# Go Service Example

Modern and minimal Go HTTP service example deployable as: Linux systemd socket activated service, NixOS module service, Docker container or Kubernetes static config or Helm chart.
